_target_: diffcsp.pl_modules.diffusion_w_type.CSPDiffusion
time_dim: 256
latent_dim: 0
cost_coord: 1.
cost_lattice: 1.
cost_type: 20.
max_neighbors: 30  # maximum number of neighbors for OTF graph bulding in decoder
radius: 8.  # maximum search radius for OTF graph building in decoder
timesteps: 1000
coord_type: frac

defaults:
  - decoder: cspnet
  - beta_scheduler: cosine
  - sigma_scheduler: wrapped
